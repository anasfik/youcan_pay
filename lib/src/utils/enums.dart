enum YouCanPayBalanceHistorySortField {
  amount,
  causer_type,
}

enum YouCanPaySortOrder {
  asc,
  desc,
}

enum YouCanPayTransfersSortField {
  amount,
  name,
  active,
  status,
  due_by,
  created_at,
}
